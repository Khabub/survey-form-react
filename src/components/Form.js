import React from "react";
import classes from "./Form.module.scss";

const Form = () => {

  return (
    <div className={classes.container}>
      <label>Name</label>
      <input></input>
    </div>
  );
};

export default Form;